# editor-diff

Line- and character-level diffs on top of [`similar`](https://docs.rs/similar). Provides [`compute_line_diff`](crate::compute_line_diff), inline diff document building, and hunk apply helpers for git/AI review flows (M17+).
