# `editor-git`

Read-only git integration (M18): repository discovery, current branch, per-path worktree status vs `HEAD`, and `HEAD` blob contents for diff-vs-HEAD.

Writes to the repository (commit, stage, …) are intentionally out of scope.
